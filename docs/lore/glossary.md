# Glossary — Lore ↔ Technical Map

This glossary keeps lore “low-profile”: it adds meaning, but always maps back to concrete artifacts.

## Entities

- **WB::THE ORACLE** → the DetectLab pipeline (CLI + artifacts + determinism gates)
- **WB::REPLICATORS** → credential-stuffing / spray-like attack playbooks in SKYNET
- **WB::THE_MULE** → low-and-slow progression campaigns in SKYNET
- **WB::THE_CHAMELEON** → adaptive automation playbooks in SKYNET

## Trials (evaluation)
- **Time Trial** → `time_eval` split
- **Stranger Trial** → `user_holdout` split
- **Integrity Trial** → `manifest.json` content hashes + RR two-run signatures

## Artifacts
- **Run Vault** → `runs/<run_id>/`
- **Ledger** → `manifest.json`
- **Testimony** → `report.md` + `summary.md`
