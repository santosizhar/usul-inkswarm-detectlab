# Changelog

All notable changes to **Inkswarm DetectLab** will be documented in this file.

The format is inspired by *Keep a Changelog* and uses semantic-ish versioning.

## Unreleased

- RC-0001: Repo declutter + docs/journals consolidation + release hygiene pass.

## 0.1.0 — 2025-12-20

- MVP pipeline scaffold (Skynet synthetic telemetry → dataset → features → baselines → eval → reporting).
- CLI entrypoint (`detectlab`) + config system.
- MkDocs documentation set + ceremony/journal history.
